package services

func WriteToken(userId string, token string) {
	// TODO: create logic to save token to redis
}

func DeleteToken(userId string, token string) {
	// TODO: create logic to delete token from redis
}
